name: html_report
description: >
   This template is to be used for any output format. HTML styling is provided by the css in the output_styling_only template (this ensures only 1 file, rmd_template skeleton, needs to be saved within your project).
create_dir: FALSE
